<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coursework">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mobile_reminder"></activity>
        <activity android:name=".pop_up" />
        <activity android:name=".Object_Orientated_Programming" />
        <activity android:name=".Computer_science" />
        <activity android:name=".Web" />
        <activity android:name=".Courses" />
        <activity android:name=".Reminders" />
        <activity android:name=".launch" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Pop"
            android:theme="@style/AppTheme.custom_theme"></activity>
        <activity android:name=".pop_win" />
        <receiver android:name=".AlarmReceiver"/>
    </application>

</manifest>